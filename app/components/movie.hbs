<div class="px-4 pt-1 pb-2 w-full md:w-1/2 lg:w-1/3 xl:w-1/4">
  <h2 class="font-extrabold text-center text-xl">{{ @movie.Title }}</h2>
  <div>
    <img
      width="200"
      src={{ this.poster }}
      alt="The movie titled: {{ @movie.Title }}"
      class="mx-auto">
  </div>
  <p class="text-center">({{ @movie.Year }})</p>
</div>
